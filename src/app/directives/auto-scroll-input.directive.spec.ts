import { AutoScrollInputDirective } from './auto-scroll-input.directive';

describe('AutoScrollInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoScrollInputDirective();
    expect(directive).toBeTruthy();
  });
});
